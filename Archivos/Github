Configuración Github en linux

**actualizar o instalar git

sudo apt update
sudo apt install git

git config --global user.name "jmrivera82"
git config --global user.email "jmrivera82@gmail.com"

**para chequear
git config --global --list

ssh-keygen -t ed25519 -C "jmrivera82@gmail.com"

eval "$(ssh-agent -s)"

ssh-add ~/.ssh/id_ed25519

**Agregar clave SSH en página de github

**Verificar conexón con
ssh -T git@github.com

git clone git@github.com:jmrivera82/ReactFlex75915.git

Otros comandos:

git add .
git branch -M main
git commit -m "mensaje"
git push -u origin main

git log --oneline

git diff archivo ** para ver los cambios realizados

git restore --source hash archivo ** par restaurar a una versión anterior

[Git - Github]$ git branch
* main
git checkout -b desarrollo
